name: cloudflare-iac
description: DNS configuration with Cloudflare
runtime:
  name: python
  options:
    toolchain: uv
    virtualenv: .venv
    # typechecker: pyright
    # handle this ourselves with pre-commit
